@inject IStateService StateService
@implements IDisposable

<div class="top-menu">
    <img class="topImg" src="img/luckyCoin.png" style ="width : 60px; height: 50px;" />
    <span class = "@StateService.CoinCss" >@StateService.TotalCoin</span>
</div>

@code {
  //  string cssClass => StateService.CoinCss;
    protected override void OnInitialized()
    { 
        StateService.OnChange += StateHasChanged;
        
    }

    public void Dispose()
    {

        StateService.OnChange -= StateHasChanged;

    }
}
